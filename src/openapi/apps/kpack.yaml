AppKpack:
  additionalProperties: false
  type: object
  properties:
    id:
      $ref: ../definitions.yaml#/idName
    enabled:
      type: boolean
      default: false
    rawValues:
      $ref: ../definitions.yaml#/rawValues
      x-externalDocsPath: docs/apps/trivy-operator
      type: object
    values:
      x-externalDocsPath: docs/apps/velero
      additionalProperties: false
      type: object
      properties:
        buildPacks:
          title: Build Packs
          description: Buildpackages from the Paketo project that can be used by Kpack to build OCI images.
          properties:
            java:
              properties:
                enabled:
                  type: boolean
                  default: true
            nodejs:
              properties:
                enabled:
                  type: boolean
                  default: true
            go:
              properties:
                enabled:
                  type: boolean
                  default: true
            dotnetCore:
              properties:
                enabled:
                  type: boolean
                  default: false
            php:
              properties:
                enabled:
                  type: boolean
                  default: false
  required: [id]


