AppKubeapps:
  additionalProperties: false
  x-externalDocsPath: https://kubeapps.com
  x-short: Your Application Dashboard for Kubernetes
  description: Kubeapps is a web-based UI for deploying and managing applications in Kubernetes clusters
  title: Kubeapps
  type: object
  properties:
    id:
      $ref: ../definitions.yaml#/idName
    enabled:
      default: false
      type: boolean
    rawValues:
      $ref: ../definitions.yaml#/rawValues
    shortcuts:
      description: Resource URL that lands the user in a place of interest within an app.
      type: array
      nullable: true
      items:
        $ref: ../definitions.yaml#/shortcut
    values:
      x-externalDocsPath: docs/console/apps/kubeapps
      type: object
      properties:
        postgresqlPassword:
          type: string
          description: Once set and deployed it cannot be changed with manual intervention.
          x-secret: 'randAlphaNum 20'
      required:
        - postgresqlPassword
