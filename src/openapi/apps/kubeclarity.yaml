AppKubeclarity:
  additionalProperties: false
  x-externalDocsPath: https://github.com/openclarity/kubeclarity
  x-short: A runtime security scanner for container images
  description: KubeClarity is a tool for detection and management of Software Bill Of Materials (SBOM) and vulnerabilities of container images and filesystems.
  x-info: KubeClarity provides real-time image scanning to help Otomi admins secure their clusters.
  title: KubeClarity
  type: object
  properties:
    id:
      $ref: ../definitions.yaml#/idName
    enabled:
      type: boolean
      x-default: false
    rawValues:
      $ref: ../definitions.yaml#/rawValues
    shortcuts:
      description: Resource URL that lands the user in a place of interest within an app.
      type: array
      nullable: true
      items:
        $ref: ../definitions.yaml#/shortcut
    values:
      x-externalDocsPath: docs/console/apps/kubeclarity
      type: object
      properties:
        resources:
          type: object
          properties:
            limits:
              type: object
              properties:
                cpu:
                  $ref: ../definitions.yaml#/quantityCpu
                  x-default: 1000m
                memory:
                  $ref: ../definitions.yaml#/quantityMem
                  x-default: 1000Mi
            requests:
              type: object
              properties:
                cpu:
                  $ref: ../definitions.yaml#/quantityCpu
                  x-default: 100m
                memory:
                  $ref: ../definitions.yaml#/quantityMem
                  x-default: 200Mi
              required:
                - limits
                - requests
        databasePassword:
          type: string
          description: Once set and deployed it cannot be changed with manual intervention.
          x-secret: 'randAlphaNum 20'
      required:
        - databasePassword
