Cloud:
  x-acl:
    admin: [read-any]
    team: [read-any]
  properties:
    name:
      type: string
      description: A cluster name
      readOnly: true
    clusters:
      type: array
      items:
        $ref: 'cluster.yaml#/Cluster'
    domain:
      type: string
      description: A fqdn for the cloud
