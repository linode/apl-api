Cluster:
  additionalProperties: false
  type: object
  x-acl:
    platformAdmin:
      - read-any
      - update-any
    teamAdmin:
      - read-any
    teamMember:
      - read-any
  title: Cluster
  description: The Kubernetes cluster running APL.
  properties:
    name:
      $ref: definitions.yaml#/clusterName
    domainSuffix:
      $ref: definitions.yaml#/domainSuffix
    apiServer:
      $ref: definitions.yaml#/url
      title: API server
      description: URL to the Kubernetes API. Used to generate the KUBECONFIG for download, for direct access to the cluster.
    k8sContext:
      type: string
      x-hidden: true
    provider:
      $ref: definitions.yaml#/provider
