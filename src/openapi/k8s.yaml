K8sService:
  x-acl:
    platformAdmin: [read-any]
    teamAdmin: [read]
    teamMember: [read]
  properties:
    name:
      type: string
    ports:
      type: array
      items:
        type: integer
    managedByKnative:
      type: boolean
      default: false
  required:
    - name
