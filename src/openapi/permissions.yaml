TeamPermissions:
  type: object
  properties:
    service:
      type: array
      items:
        type: string
        enum: [ingress]
      uniqueItems: true
    team:
      type: array
      items:
        type: string
        enum: [alerts, oidc, resourceQuota]
      uniqueItems: true
