TeamSelfService:
  type: object
  properties:
    Service:
      type: array
      items:
        type: string
        enum: [ingress]
      uniqueItems: true
    Team:
      type: array
      items:
        type: string
        enum: [alerts, oidc, resourceQuota]
      uniqueItems: true
  required:
    - Team
    - Service
