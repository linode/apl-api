Session:
  properties:
    cluster:
      $ref: cluster.yaml#/Cluster
    clusters:
      type: array
      items:
        $ref: cluster.yaml#/AdditionalCluster
    dns:
      $ref: definitions.yaml#/dns_
      readOnly: true
    core:
      type: object
      readOnly: true
    isDirty:
      type: boolean
      readOnly: true
    namespaces:
      readOnly: true
      type: array
      items:
        type: string
      uniqueItems: true
    teams:
      readOnly: true
      type: array
      items:
        $ref: team.yaml#/Team
    user:
      $ref: user.yaml#/User
    versions:
      properties:
        core: 
          type: string
        api: 
          type: string
